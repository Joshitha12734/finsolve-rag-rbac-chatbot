# Core backend
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2

# LLM (Groq is the default provider — fast, free tier; see backend/llm_client.py)
groq>=0.11.0
# Anthropic is optional — only needed if you set LLM_PROVIDER=anthropic
anthropic==0.34.2

# Retrieval - TF-IDF fallback (always available)
scikit-learn==1.5.2
pandas==2.2.3

# Structured query (SQL) agent
duckdb>=1.1.1
tabulate==0.9.0

# Hybrid retrieval (lexical signal)
rank_bm25==0.2.2

# Retrieval - preferred embedding backend (optional but recommended)
# First run downloads the all-MiniLM-L6-v2 model, so needs internet access once.
# If you hit "Cannot copy out of meta tensor" on first run, it's a known
# torch/accelerate version mismatch issue (unrelated to this app's code) —
# try: pip install -U torch sentence-transformers accelerate
chromadb==0.5.5
sentence-transformers==3.1.1

# Frontend
streamlit==1.38.0
requests==2.32.3

python-dotenv==1.0.1

# Testing
pytest==8.3.3
